---
phase: 01-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - "src/app/[locale]/client.tsx"
  - "src/app/components/SplitPane/SplitPaneView.tsx"
  - "src/app/components/SplitPane/index.ts"
autonomous: true
requirements:
  - SBS-01
  - SBS-02
  - SBS-03
must_haves:
  truths:
    - "用户可以看到左右分屏布局，左侧显示原文输入区，右侧显示译文显示区"
    - "用户可以通过拖拽分隔线调整左右面板宽度"
    - "面板比例在页面刷新后保持不变"
  artifacts:
    - path: "src/app/components/SplitPane/SplitPaneView.tsx"
      provides: "分屏预览 Tab 视图容器"
      min_lines: 60
    - path: "src/app/[locale]/client.tsx"
      provides: "Tab 切换逻辑"
      exports: ["ClientPage"]
  key_links:
    - from: "client.tsx"
      to: "SplitPaneView"
      via: "Tab 内容渲染"
      pattern: "SplitPaneView"
    - from: "SplitPaneView"
      to: "SplitPaneContainer"
      via: "组件引用"
      pattern: "SplitPaneContainer"
---

<objective>
将 SplitPaneContainer 集成到 Tab 系统中，创建"分屏预览"Tab 并实现完整的文本输入/显示流程。

Purpose: 实现 Phase 1 的用户可见功能 — 新的分屏预览 Tab，包含原文输入和译文显示。
Output: 完整的分屏预览 Tab 功能，用户可输入原文并触发翻译。
</objective>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@src/app/[locale]/client.tsx
@src/app/components/SplitPane/SplitPaneContainer.tsx
@src/app/hooks/useTranslationContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 读取 client.tsx 了解 Tab 结构</name>
  <files>src/app/[locale]/client.tsx</files>
  <read_first>src/app/[locale]/client.tsx</read_first>
  <action>
    阅读 client.tsx 文件，了解:
    1. 现有的 Tab 结构是如何组织的
    2. TranslationSettings 组件是如何被使用的
    3. Tab 切换的 state 是如何管理的

    注意具体实现细节以便在 Task 2 中正确添加 SplitPaneView Tab。
  </action>
  <verify>
    <automated>test -f "src/app/[locale]/client.tsx" && echo "READ"</automated>
  </verify>
  <done>已理解 client.tsx 的 Tab 结构</done>
</task>

<task type="auto">
  <name>Task 2: 创建 SplitPaneView 组件</name>
  <files>src/app/components/SplitPane/SplitPaneView.tsx</files>
  <action>
    创建 `src/app/components/SplitPane/SplitPaneView.tsx`:

    根据 CONTEXT.md 的决策:
    - D-03: 独立源输入 — 分屏视图有自己独立的文本输入区域
    - D-02: 设置来自"翻译区" — 使用 useTranslationContext 获取翻译设置

    ```typescript
    "use client";

    import React, { useState, useCallback } from "react";
    import { Card, Button, Typography, Input, Space, Flex, App } from "antd";
    import { GlobalOutlined } from "@ant-design/icons";
    import { useTranslations } from "next-intl";
    import { useTranslationContext } from "@/app/components/TranslationContext";
    import { SplitPaneContainer } from "./index";

    const { TextArea } = Input;
    const { Text } = Typography;

    interface SplitPaneViewProps {
      /** 触发翻译的回调函数 */
      onTranslate?: () => void;
    }

    /**
     * 分屏预览视图组件
     * - 左侧: 原文输入区 (TextArea)
     * - 右侧: 译文显示区 (只读 TextArea)
     * - 集成 SplitPaneContainer 实现拖拽分隔
     */
    const SplitPaneView: React.FC<SplitPaneViewProps> = ({ onTranslate }) => {
      const t = useTranslations("common");
      const { message } = App.useApp();

      // 分屏视图独立的原文文本状态
      const [sourceText, setSourceText] = useState("");

      // 从 useTranslationContext 获取翻译结果 (只读显示)
      // 注意: 这里实际上需要使用 translationContext 的 translatedText
      // 但由于分屏视图是独立输入，分屏视图需要自己调用翻译
      const {
        translatedText,
        translateInProgress,
        translationMethod,
        sourceLanguage,
        targetLanguage,
        translateContent,
        getCurrentConfig,
      } = useTranslationContext();

      // 处理翻译按钮点击
      const handleTranslate = useCallback(async () => {
        if (!sourceText.trim()) {
          message.warning(t("noSourceText"));
          return;
        }

        const config = getCurrentConfig();
        if (!config.apiKey && !config.freeApiEnabled) {
          message.error(t("noApiKey"));
          return;
        }

        // 调用翻译
        try {
          const lines = sourceText.split("\n");
          const translatedLines = await translateContent(lines, translationMethod, targetLanguage);
          // 翻译结果会通过 useTranslationContext 自动更新 translatedText
        } catch (error) {
          message.error(t("translationError"));
        }
      }, [sourceText, translateContent, translationMethod, targetLanguage, getCurrentConfig, message, t]);

      // 左侧面板: 原文输入
      const leftPanel = (
        <Card className="h-full" title={t("sourceArea")}>
          <Flex vertical className="h-full" gap="small">
            <TextArea
              value={sourceText}
              onChange={(e) => setSourceText(e.target.value)}
              placeholder={t("pasteUploadContent")}
              rows={12}
              className="flex-1"
              aria-label={t("sourceArea")}
            />
            <Flex justify="end">
              <Text type="secondary" className="!text-xs">
                {sourceText.length} {t("charLabel")} / {sourceText.split("\n").length} {t("lineLabel")}
              </Text>
            </Flex>
          </Flex>
        </Card>
      );

      // 右侧面板: 译文显示
      const rightPanel = (
        <Card className="h-full" title={t("translationResult")}>
          <Flex vertical className="h-full" gap="small">
            <TextArea
              value={translatedText}
              readOnly
              placeholder={t("translationResult")}
              rows={12}
              className="flex-1"
              aria-label={t("translationResult")}
            />
            <Flex justify="end">
              <Text type="secondary" className="!text-xs">
                {translatedText.length} {t("charLabel")} / {translatedText.split("\n").length} {t("lineLabel")}
              </Text>
            </Flex>
          </Flex>
        </Card>
      );

      return (
        <div className="h-full">
          {/* 翻译操作栏 */}
          <Flex justify="space-between" align="center" className="mb-3">
            <Text type="secondary">
              {t("sourceArea")}: {sourceLanguage} → {targetLanguage}
            </Text>
            <Button
              type="primary"
              icon={<GlobalOutlined spin={translateInProgress} />}
              onClick={handleTranslate}
              disabled={translateInProgress || !sourceText.trim()}
              loading={translateInProgress}
            >
              {t("translate")}
            </Button>
          </Flex>

          {/* 分屏容器 */}
          <SplitPaneContainer
            leftPanel={leftPanel}
            rightPanel={rightPanel}
            leftTitle={t("sourceArea")}
            rightTitle={t("translationResult")}
          />
        </div>
      );
    };

    export default SplitPaneView;
    ```

    **关键实现点:**
    - SplitPaneView 是分屏预览 Tab 的完整视图组件
    - 左侧面板: 使用 TextArea 输入原文 (D-03: 独立源输入)
    - 右侧面板: 显示翻译结果 (只读)
    - 翻译按钮触发翻译，调用 useTranslationContext 的翻译逻辑 (D-02: 设置来自翻译区)
    - 使用 SplitPaneContainer 实现拖拽分隔 (Phase 1 核心功能)
  </action>
  <verify>
    <automated>test -f "src/app/components/SplitPane/SplitPaneView.tsx" && echo "CREATED"</automated>
  </verify>
  <done>SplitPaneView 组件已创建</done>
</task>

<task type="auto">
  <name>Task 3: 集成到 Tab 系统</name>
  <files>src/app/[locale]/client.tsx</files>
  <action>
    修改 client.tsx 添加"分屏预览"Tab:

    1. 导入 SplitPaneView 组件
    2. 在 Tab items 中添加 "分屏预览" 选项
    3. Tab 内容根据 activeKey 渲染对应的组件

    具体修改:
    - 在 imports 中添加 SplitPaneView
    - 在 Tab items 数组中添加新的 item:
      ```
      {
        key: "split",
        label: "分屏预览",
        children: <SplitPaneView />
      }
      ```

    注意: 只需要添加 Tab 定义，不需要修改 TranslationSettings 或其他 Tab 的内容。
    分屏预览 Tab 使用 existing Tab 的设置 (D-02)。
  </action>
  <verify>
    <automated>grep -q "SplitPaneView" "src/app/[locale]/client.tsx" && echo "INTEGRATED"</automated>
  </verify>
  <done>"分屏预览"Tab 已添加到 client.tsx，用户可以在 Tab 切换中看到新 Tab</done>
</task>

</tasks>

<verification>
- [ ] `src/app/components/SplitPane/SplitPaneView.tsx` 存在
- [ ] `client.tsx` 导入 SplitPaneView
- [ ] "分屏预览" Tab 已添加到 Tab items
- [ ] Tab 切换可以显示 SplitPaneView
- [ ] SplitPaneView 包含左侧原文输入和右侧译文显示
- [ ] 翻译按钮调用翻译逻辑
</verification>

<success_criteria>
- 用户可以在 Tab 栏看到"分屏预览"选项
- 点击"分屏预览"Tab 显示分屏视图
- 分屏视图左侧可输入原文
- 右侧显示翻译结果 (来自 useTranslationContext)
- 拖拽分隔线可调整宽度 (由 SplitPaneContainer 提供)
- 刷新页面后分屏比例保持不变 (由 useLocalStorage 提供)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
